version: "3.9"

networks:
  forge1_net:
    name: forge1_net

volumes:
  pgdata:

services:
  db:
    image: ankane/pgvector:latest
    container_name: forge1_db
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-forge}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-forge}
      - POSTGRES_DB=${POSTGRES_DB:-forge}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-forge}"]
      interval: 10s
      timeout: 5s
      retries: 5
    # no host port exposure to avoid conflicts
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - forge1_net

  redis:
    image: redis:7-alpine
    container_name: forge1_redis
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    # no host port exposure to avoid conflicts
    networks:
      - forge1_net

  api:
    build: .
    container_name: forge1_api
    env_file:
      - .env.docker
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - ENV=${ENV:-dev}
      - JWT_SECRET=${JWT_SECRET}
    ports:
      - "8001:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - forge1_net


